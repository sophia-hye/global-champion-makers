# Supabase 연동 (상담 신청 저장 + Admin)
# Supabase 프로젝트 → Settings → API 에서 값 복사 후 .env.local 로 저장하세요.
# 값이 없으면 상담 폼은 프론트엔드 접수 확인만 표시(저장 안 함)하고, /admin 은 안내만 표시됩니다.

NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY
